<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                                   android:layout_width="match_parent"
                                                   android:layout_height="wrap_content">

    <ImageView
            android:id="@+id/fb_login_btn"
            android:layout_width="60dp"
            android:layout_height="@dimen/login_item_size"
            android:layout_marginBottom="8dp"
            android:layout_marginTop="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/google_sign_in_btn"
            app:layout_constraintHorizontal_bias="0.72"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_fb"/>

    <ImageView
            android:id="@+id/google_sign_in_btn"
            android:layout_width="60dp"
            android:layout_height="@dimen/login_item_size"
            android:layout_marginBottom="8dp"
            android:layout_marginTop="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/iv_twitter_sign_in_btn"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toEndOf="@+id/fb_login_btn"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_google"/>

    <ImageView
            android:id="@+id/iv_twitter_sign_in_btn"
            android:layout_width="60dp"
            android:layout_height="@dimen/login_item_size"
            android:layout_marginBottom="8dp"
            android:layout_marginTop="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toEndOf="@+id/google_sign_in_btn"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_twitter"/>
</androidx.constraintlayout.widget.ConstraintLayout>